<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'PayEat', orders_path, class: "navbar-brand" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <% if current_user %>
        <div class="col-sm-4 col-sm-offset-4">
          <%= link_to '<i class="fa fa-plus"></i>&nbsp;&nbsp;New Order'.html_safe, new_order_url, class: "btn btn-info
          navbar-btn" %>
        </div>
        <ul class="nav navbar-nav navbar-right">


          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= current_user.email %><span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to user_path(current_user.id) do %><i class="fa fa-user"></i> My profile<% end %></li>
              <li><%= link_to edit_user_registration_url do %><i class="fa fa-pencil-square-o"></i> Edit profile<% end %></li>
              <li role="separator" class="divider"></li>
              <li><%= link_to destroy_user_session_path, method: :delete do %><i class="fa fa-sign-out"></i> Logout<% end %></li>
            </ul>
          </li>
        </ul>
        <% else %>
        <ul class="nav navbar-nav navbar-right">

          <%= link_to '<i class="fa fa-sign-in"></i>&nbsp;&nbsp;Login'.html_safe,new_user_session_url, class: "btn btn-success navbar-btn" %>

          <%= link_to '<i class="fa fa-user-plus"></i>&nbsp;&nbsp;Sign Up'.html_safe, new_user_registration_url, class: "btn btn-primary navbar-btn" %>
        </ul>
      <% end %>

    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
